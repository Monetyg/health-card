# 健康证 H5 免费公网部署方案

> 目标：手机在任意网络（4G/5G/异地 WiFi）下扫码可查验，全免费。

## 1. 总览

| 端 | 免费平台 | 说明 |
|---|---|---|
| 前端 `frontend/` | Vercel | 免费额度大、国内可访问、有二级域名、自动 HTTPS |
| 后端 `backend/` | Render 免费 Web Service | 免费 750h/月，支持 Node + `PORT` 环境变量 |
| 数据库 | Neon 免费 Postgres | 0.5GB 免费，替代本地 JSON/SQLite，Render 重启不丢数据 |
| 图片 | Cloudinary 免费版 | 25GB/月流量，`uploads/` 本地目录改为直传 URL |

链路：手机扫码 → `https://<vercel域名>/v/<verifyId>` → 前端调 `https://<render域名>/api/verify/<verifyId>` → Neon + Cloudinary 返回数据。

## 2. 前端改造（Vercel）

1. 新增 `frontend/vercel.json`：

```json
{
  "rewrites": [{ "source": "/v/:id", "destination": "/" }, { "source": "/cert/:id", "destination": "/" }]
}
```

解决 `/v/:id` 直连 404。

2. `src/config/publicUrl.ts` 改为读环境变量：

```ts
export function getVerifyBaseUrl() {
  return (import.meta.env.VITE_VERIFY_BASE_URL || location.origin).replace(/\/$/, '');
}
export function getApiBase() {
  return (import.meta.env.VITE_API_BASE_URL || '').replace(/\/$/, '');
}
```

查验页 `fetch` 改为 `` `${getApiBase()}/api/verify/${id}` ``，照片已是 Cloudinary 绝对 URL，无需拼接。

3. Vercel 面板环境变量：

- `VITE_VERIFY_BASE_URL=https://health-card.vercel.app`
- `VITE_API_BASE_URL=https://health-card-api.onrender.com`

4. 部署：Vercel → New Project → 选仓库 `root=frontend` → `npm run build` → 绑定域名。

## 3. 后端改造（Render）

1. 新增 `backend/Dockerfile`：

```dockerfile
FROM node:20-alpine
WORKDIR /app
COPY package.json ./
RUN npm install --production
COPY . .
CMD ["node", "src/index.js"]
```

2. `src/index.js`：`PORT` 取 `process.env.PORT`，`cors()` 放开 Vercel 域名。
3. 照片：`multer` 改内存存储 → 直传 Cloudinary，`photoUrl` 存 `secure_url`。
4. 数据库：Prisma `provider` 由 `sqlite` 切 `postgresql`，`DATABASE_URL` 取 Neon 连接串；`genCertNo` 保持 `JK+年份+随机+流水`，`verifyId=nanoid(10)`。
5. Render 面板：New Web Service → `Docker` → Health Check Path `/api/health` → 环境变量 `DATABASE_URL`、`CLOUDINARY_URL`。

## 4. 联调清单

- [ ] 电脑提交一张证，手机 4G 下扫领证页二维码
- [ ] 查验页显示编号/姓名脱敏/医院/有效期/照片
- [ ] 扫录入页“待签发”预览码应提示不可查验
- [ ] Render 重启后历史证仍可查（Neon 生效）

## 5. 费用与限制

- 全免费；Render 免费实例 15 分钟无请求会休眠，首次扫码慢 30~50s，可加 UptimeRobot 每 10 分钟 ping `/api/health` 保活。
- 境外节点微信扫码可直接打开；如需小程序/公众号内嵌或国内备案域名，后续再套已备案域名反代。

## 6. 一句话执行顺序

Vercel 先上前端 → Neon 建库 → 后端接 Cloudinary 上 Render → 回填两个环境变量重新部署 → 手机断 WiFi 用流量扫码验证。
